{
  "name": "projet-3",
  "version": "1.0.0",
  "description": "Site Ohmyfood",
  "scripts": {
    "sass": "sass --watch sass:css --no-source-map",
    "sass-compressed": "sass --watch sass:css --style compressed --no-source-map",
    "live-server": "live-server",
    "sass-live-server": "npm-run-all --parallel live-server sass",
    "prefix" : "postcss css/style.css --use autoprefixer -d css/prefixed/ --no-map"
  },
  "author": "Jyhad Ben Youssef",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^10.4.2",
    "postcss": "^8.4.7",
    "postcss-cli": "^9.1.0",
    "sass": "^1.49.8"
  },
  "browserslist": "last 4 versions"
}
